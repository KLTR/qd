FROM node:alpine

WORKDIR /app

ENV PATH /usr/src/app/node_modules/.bin:$PATH

COPY ./package.json /app
RUN npm set registry http://172.23.4.23:4873

RUN cd /app && npm install

RUN cd /app && npm install @common/intels@0.0.4 --no-audit

COPY . /app
RUN cd /app && npm run build-prod
